{
  "name": "alvynle-website",
  "version": "1.0.0",
  "description": "This follows a pattern to deploy a react website",
  "scripts": {
    "buildwebsite": "cd website && yarn && yarn build && cd ../",
    "buildcdk": "cd cdk && yarn && yarn run build && cd ../",
    "build": "yarn run buildwebsite && yarn run buildcdk",
    "deploy": "cd cdk && yarn run deploy"
  },
  "author": "",
  "license": "ISC"
}
